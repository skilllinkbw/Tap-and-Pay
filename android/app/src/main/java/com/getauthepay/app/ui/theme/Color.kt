package com.getauthepay.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * AuthePay brand palette (bank-grade).
 *
 * The deep navy `#0B1B2F` is the primary surface used on the launcher icon
 * and splash background. The mid-blue `#2B6CB0` is the action colour used
 * for the accept-payment button and the official logo mark. Accents and
 * semantic colours are tuned for high-contrast accessibility (WCAG AA at
 * 18pt+ and the primary text on each background).
 */
val BrandNavy = Color(0xFF0B1B2F)
val BrandNavyDeep = Color(0xFF071320)
val BrandBlue = Color(0xFF2B6CB0)
val BrandBlueMid = Color(0xFF4A7AB5)
val BrandBlueSoft = Color(0xFFA0C4E8)

val AccentTeal = Color(0xFF00BFA5)
val SurfaceWhite = Color(0xFFFFFFFF)
val BackgroundNeutral = Color(0xFFF5F7FA)
val SurfaceMuted = Color(0xFFEEF2F6)
val DividerSubtle = Color(0xFFE2E8F0)

val TextPrimary = Color(0xFF1A2330)
val TextSecondary = Color(0xFF5A6472)
val TextOnBrand = Color(0xFFFFFFFF)

val SuccessGreen = Color(0xFF2F855A)
val WarningAmber = Color(0xFFB7791F)
val DangerRed = Color(0xFFC53030)

val SandboxAmber = Color(0xFFB7791F)
val SandboxBackground = Color(0xFFFFF7E6)